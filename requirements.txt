httpx>=0.27
feedparser>=6.0
beautifulsoup4>=4.12
lxml>=5.0

# LLM sağlayıcıları - özetleme + önem skorlama (src/summarizer.py).
# Varsayılan sağlayıcı Gemini'dir (config.yaml > summarizer.llm_provider);
# Anthropic paketi de tek satırlık "anthropic"a geri dönüş için tutulur.
google-genai>=1.0
anthropic>=0.40

PyYAML>=6.0
python-dotenv>=1.0
APScheduler>=3.10
python-dateutil>=2.9

# NewsAPI.ai (Event Registry) resmi Python SDK'sı — Reuters/Bloomberg
# içeriğine lisanslı erişim için (bkz. src/fetchers/licensed_aggregator.py
# ve README > "Lisanslı Kaynak: Reuters/Bloomberg (NewsAPI.ai)")
eventregistry>=9.1

# Veritabanı (haberler, özetler, önem skorları, bildirim durumu) - src/db.py
SQLAlchemy>=2.0

# Postgres sürücüsü - YALNIZCA DATABASE_URL ortam değişkeni tanımlıysa devreye
# girer (bkz. src/db.py > init_db, README > "Vercel'e Deploy - Hibrit
# Mimari"). DATABASE_URL tanımsızsa uygulama yerel SQLite kullanmaya devam
# eder, bu paket hiç import edilmez - varlığı zararsızdır.
psycopg2-binary>=2.9

# Telegram bildirimleri - src/notifier.py
python-telegram-bot>=21.0

# Web dashboard - src/web/app.py
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1

# Web Push bildirimleri (sekme/tarayıcı KAPALIYKEN bile çalışan tarayıcı
# bildirimleri) - src/web_push.py. VAPID imzalama + mesaj şifrelemesi
# (RFC 8291/8292) dahil, ek bir servis/API anahtarı GEREKMEZ (py-vapid
# bağımlılığı ile anahtar çifti kendi kendine üretilir, bkz. src/web_push.py
# > _get_or_create_vapid_keys).
pywebpush>=2.0

# IANA zaman dilimi veritabanı - haber saatlerini Türkiye saatine çevirmek
# için (bkz. src/timezone_utils.py > to_turkey_time, zoneinfo.ZoneInfo
# kullanır). Şu ana kadar (muhtemelen pandas gibi başka bir bağımlılık
# üzerinden) DOLAYLI olarak kurulu olsa da, doğrudan/açık bir bağımlılık
# OLARAK burada listelenmesi, gelecekte bağımlılık zinciri değişse bile
# (ör. requirements.txt yeniden üretilirse) zoneinfo'nun Linux/Render
# ortamında güvenilir çalışmasını garanti eder.
tzdata>=2024.1

# Opsiyonel: JS ile render edilen siteler için (config.yaml'da render_js: true
# yapılırsa gereklidir). Varsayılan olarak kurulu değildir.
# playwright>=1.45

# Opsiyonel: KAP (kap.org.tr) gibi doğrudan scrape edilemeyen kaynaklardan
# anlık bildirim almak için Telegram kanal dinleyicisi (bkz.
# src/fetchers/telegram_listener.py, README > "Event-Driven Ingestion: KAP
# Bildirimleri"). Kullanıcının KENDİ Telegram hesabıyla giriş yapan bir
# "user client" kütüphanesidir (bot API'sinden FARKLI) - yalnızca dinleyiciyi
# çalıştırmak isterseniz kurun. Varsayılan olarak kurulu değildir.
# telethon>=1.36
